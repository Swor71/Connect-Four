export function getPlayerColor(playerNumber: number) {
  switch (playerNumber) {
    case 0:
      return 'ivory';
    case 1:
      return 'red';
    case 2:
      return 'yellow';

    default:
      break;
  }
}
